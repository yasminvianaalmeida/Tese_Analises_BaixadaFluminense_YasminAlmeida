# Dependencias para RODAR A INTERFACE e as duas analises (instrumentos e LDO/LOA).
# A interface em si (app.py) usa apenas tkinter, que ja vem com o Python no Windows.
# As bibliotecas abaixo sao usadas pelos scripts de analise chamados pela interface.
pymupdf>=1.23
openpyxl>=3.1
pytesseract>=0.3.10
pillow>=10.0
rapidfuzz>=3.0
pandas>=2.0
scikit-learn>=1.3
python-docx>=1.1
